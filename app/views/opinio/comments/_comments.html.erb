<h3> Comments </h3>
<div>
  <%= paginate comments %>
</div>

<ul id="comments" class="chats">
  <% unless comments.empty? %>
    <%= render :partial => 'opinio/comments/comment', :collection => comments %>
  <% else %>
    <li id="no_comments"><%= t('opinio.messages.no_comments_found') %></li>
  <% end %>
</ul>

<div>
  <%= paginate comments %>
</div>
