<%= form_for(@deck) do |f| %>
  <% if @deck.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deck.errors.count, "error") %> prohibited this deck from being saved:</h2>

      <ul>
      <% @deck.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="deckfield">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="oppfield">
    <%= f.label "Class" %> <br>
    <%= f.select :race, options_for_select(%w[Druid Hunter Mage Paladin Priest Rogue Shaman Warlock Warrior]) %>
  </div>
  <div class="cf"></div>
  <div class="actions">
    <%= f.submit "Create Deck", class: "pure-button pure-button-primary" %>
  </div>
<% end %>
