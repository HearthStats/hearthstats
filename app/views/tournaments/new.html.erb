<% set_meta_tags :title => 'New Tournament Entry',
  :description => 'New Tournament Entry',
  :keywords => 'new, entry, tournament, hearthstats, hearthstone' %>
<%= provide(:title, t('.title'))  %>
<%= provide(:section, t('.section')) %>

<%= render 'form' %>